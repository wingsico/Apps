import 'package:flutter/material.dart';

class PracticeOfXFLScreen extends StatelessWidget {
  @override
  build(BuildContext ctx) {
    return Scaffold(
      appBar: AppBar(title: Text("幸福里练习")),
    );
  }
}
